public interface PaymentMethod {
    // it is  interface  with only method signature and no-body child classes which implement the interface
    // will take care of implementations  method declared in Interface
    void pay();
}
